# OPS portable configurations

These declarative recipes describe study semantics, not a scheduler.  Supply
data and output locations through the invoking command or an ignored local
configuration.  A runner must reject an incompatible adapter capability rather
than silently weaken a protocol.

The registry in `methods/final_ten.yaml` is the complete manuscript roster.
Its stable method identifiers are shared by training, prediction exports and
the downstream analyses.
